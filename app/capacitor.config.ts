import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'controller.mobile.app',
  appName: 'mobile-controller',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
